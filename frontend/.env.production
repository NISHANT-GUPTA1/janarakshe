# Baked into `npm run build` (Vite production mode) so the deployed Slate frontend
# calls the live Catalyst AppSail backend instead of its own origin.
VITE_API_BASE=https://crimeapi-50044117510.development.catalystappsail.in
