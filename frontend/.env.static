# Loaded automatically by `vite build --mode static` (see package.json build:static).
# Makes src/api.js read the pre-exported JSON payloads under /api/*.json instead of
# calling the FastAPI backend — which is what lets the site run on a static host.
VITE_STATIC_API=1
